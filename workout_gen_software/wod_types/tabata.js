const numbers = require('../numbers');

module.exports = {
  generate: (user, wod, exercises) => {
    console.log('tabata', wod);
    /*
      - 8 rounds of 20 seconds on, 10 seconds off.

      1. choose exercises
        - different exercise in each round?
        - repeat exercises?
        - wod.exercises
      2. no reps, as many as possible in 20 seconds
      3. no run component
        - wod.run = false
      4. assign instructions
        - wod.instructions
    */
  }
};